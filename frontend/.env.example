# CivicSolve V2 Frontend Configuration
# Optional: Only needed if frontend is deployed separately from backend (e.g. Vercel frontend + Render backend)
# When deployed together on Render or behind a reverse proxy, leave this blank (defaults to /api/v1)
# VITE_API_URL=https://civicsolve-v2.onrender.com/api/v1
